
//#include <stdio.h>      // stdio (for NULL) is included in student.h
#include "student.h"
//#include <string.h>   


int main(void) 
{
    list studentList;    //** LOOK at head and tail values: if NULL.
    // char a[10];
    // printf("%zu", strlen(a));    // printed at console: 0
    printMenu();

    // printf("\nEnter the id of the student which you want to delete: ");    // OK: put as a menu choice.
    //** ++ CHECK the the input (++).
    //** ++ FUNCTION for the CHECKS !!
    // scanf("%d", st.id);      // Save the user's input as the name of the new student.
    
    return 0;
}